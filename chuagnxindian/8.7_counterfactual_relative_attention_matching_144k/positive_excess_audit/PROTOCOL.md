# Protocol

Frozen model: 8.7 Mean-CRAM C3 Stage-1 followed by vanilla Stage-2. Fixed VGG Group-A is the existing 428-sample `A_ogl_help` subset. GT, NearFP, OGL, and group labels are used only after map generation for offline analysis and are not candidate training inputs. All sample IDs are retained in `per_sample/`.
